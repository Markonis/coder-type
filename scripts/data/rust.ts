import { Repo } from "./index";

export const rustRepo:Repo={
    label:'Rust',
    url: "https://rustlang.org",
    files:[
        {
            path:"",
            code:`
            
            `
        }
    ]
}