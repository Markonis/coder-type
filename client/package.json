{
  "name": "client",
  "version": "1.0.0",
  "scripts": {
    "start": "rm -rf dist && npx parcel src/index.html",
    "build": "rm -rf dist && npx parcel build --no-source-maps src/index.html"
  },
  "devDependencies": {
    "parcel": "2.15.4",
    "typescript": "5.9.2"
  },
  "browserslist": [
    "last 2 versions and not dead"
  ]
}
